name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    # Integration tests run against a real Postgres rather than a mocked
    # client: transaction boundaries, unique-constraint races and cross-tenant
    # scoping are exactly the behaviours a mock will happily fake.
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: accubook_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    env:
      # Build-time placeholders. Real production values come from Vercel project env.
      DATABASE_URL: postgresql://placeholder@localhost:5432/db?sslmode=disable
      AUTH_SECRET: placeholder_secret_at_least_32_chars_long_x
      NEXTAUTH_URL: http://localhost:3000
      NODE_ENV: test
      # Consumed by vitest.integration.config.ts, which pins DATABASE_URL to it
      # before any app module loads.
      TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/accubook_test

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm test

      # Applies the real migration chain, so a migration that is missing,
      # misordered or broken fails here rather than on deploy day.
      - name: Migrate test database
        run: npx prisma migrate deploy
        env:
          DATABASE_URL: ${{ env.TEST_DATABASE_URL }}

      - name: Integration tests
        run: npm run test:integration

      - name: Build
        run: npm run build
